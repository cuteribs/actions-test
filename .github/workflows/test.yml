on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test action from Cuteribs
      # You may pin to the exact commit or the version.
      # uses: cuteribs/actions-test@77490c0df7a5c646e714d053296ab4a4b6ace67d
      uses: cuteribs/actions-test@v0
      with:
        # The last committer
        lastAuthor: 'ribs'
              
